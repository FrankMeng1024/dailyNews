# AI 资讯微信小程序部署指南

## 目录

1. [费用总览](#费用总览)
2. [准备工作](#准备工作)
3. [第一步：注册微信小程序](#第一步注册微信小程序)
4. [第二步：购买云服务器](#第二步购买云服务器)
5. [第三步：域名与备案](#第三步域名与备案)
6. [第四步：服务器部署](#第四步服务器部署)
7. [第五步：小程序前端开发](#第五步小程序前端开发)
8. [第六步：测试与调试](#第六步测试与调试)
9. [第七步：提交审核](#第七步提交审核)
10. [常见问题](#常见问题)

---

## 费用总览

### 方案对比

| 项目 | 微信云托管方案 | 自建服务器方案 |
|------|---------------|---------------|
| 小程序认证 | 300元/年 | 300元/年 |
| 服务器 | 50-100元/月 | **45元/月**（轻量服务器） |
| 数据库 | 30-50元/月 | **0元**（自建 MySQL） |
| 存储 | 10-30元/月 | **0元**（本地存储） |
| 域名 | 需要 | 5元/月（60元/年） |
| SSL证书 | 自动 | **0元**（Let's Encrypt） |
| **月均费用** | **115-205元** | **~70元** |
| **首年费用** | **1680-2760元** | **~900元** |

### 一次性费用

| 项目 | 费用 | 说明 |
|------|------|------|
| 小程序认证 | 300元/年 | 必须，企业主体 |
| 域名购买 | 30-60元/年 | .com 或 .cn |
| ICP备案 | 0元 | 腾讯云免费代办 |

### 微信小程序前端费用

**微信小程序前端开发和发布是免费的！**

- 开发者工具：免费
- 代码托管：免费
- 发布上线：免费
- 用户使用：免费

只有以下情况需要付费：
- 企业主体认证：300元/年（必须）
- 微信支付功能：需要商户号（你的项目不需要）
- 云开发/云托管：按量付费（你用自建服务器就不需要）

---

## 准备工作

### 你需要准备的材料

1. **营业执照**（你已有）
   - 清晰照片或扫描件
   - 确保在有效期内

2. **法人身份证**
   - 正反面照片
   - 用于小程序认证

3. **对公账户**
   - 用于打款验证（腾讯会打几分钱验证）
   - 或法人微信扫码验证

4. **手机号**
   - 接收验证码
   - 建议用法人手机号

5. **邮箱**
   - 注册小程序账号用
   - 建议用企业邮箱或新注册一个

---

## 第一步：注册微信小程序

### 1.1 注册账号

1. 访问 https://mp.weixin.qq.com
2. 点击「立即注册」
3. 选择「小程序」
4. 填写邮箱、密码
5. 邮箱验证

### 1.2 选择主体类型

选择「企业」，然后选择：
- 企业类型：根据营业执照选择
- 企业名称：与营业执照一致
- 营业执照注册号：统一社会信用代码

### 1.3 管理员信息

- 填写法人姓名、身份证号
- 法人微信扫码验证（或对公打款验证）

### 1.4 完成认证（300元/年）

1. 进入小程序后台
2. 设置 → 基本设置 → 微信认证
3. 支付 300 元认证费
4. 等待审核（1-3 个工作日）

### 1.5 获取开发信息

认证通过后，进入：
- 开发管理 → 开发设置
- 记录下：
  - **AppID**：wx1234567890abcdef
  - **AppSecret**：点击「重置」获取（只显示一次，务必保存）

### ⚠️ 注意事项

- AppSecret 只显示一次，必须立即保存
- 不要泄露 AppSecret，泄露后立即重置
- 认证每年需要续费 300 元

---

## 第二步：购买云服务器

### 2.1 推荐配置

**腾讯云轻量应用服务器**（最省钱）

| 配置 | 推荐 |
|------|------|
| CPU | 2核 |
| 内存 | 2GB |
| 硬盘 | 50GB SSD |
| 带宽 | 4-6Mbps |
| 系统 | Ubuntu 22.04 |
| 价格 | ~45元/月 |

### 2.2 购买步骤

1. 访问 https://cloud.tencent.com/product/lighthouse
2. 选择「轻量应用服务器」
3. 地域选择：**上海**（备案和访问速度）
4. 镜像选择：Ubuntu 22.04 LTS
5. 套餐选择：2核2G 4Mbps
6. 购买时长：建议先买 1 个月测试

### 2.3 新用户优惠

腾讯云新用户首年优惠很大：
- 轻量服务器 2核2G：首年约 50-100 元
- 查看活动页：https://cloud.tencent.com/act

### 2.4 服务器初始化

购买后，进入控制台：

1. 记录服务器公网 IP：如 `123.45.67.89`
2. 设置 root 密码或 SSH 密钥
3. 开放端口：
   - 22（SSH）
   - 80（HTTP）
   - 443（HTTPS）
   - 8000（后端 API，可选）

---

## 第三步：域名与备案

### 3.1 购买域名

1. 访问 https://dnspod.cloud.tencent.com
2. 搜索想要的域名，如 `ainews.com`
3. 推荐后缀：.com、.cn、.net
4. 价格：约 30-60 元/年

### 3.2 域名实名认证

购买后需要实名认证：
1. 进入域名管理
2. 上传营业执照
3. 等待审核（1-3 天）

### 3.3 ICP 备案（重要！）

**微信小程序后端必须使用已备案的域名！**

备案流程：
1. 进入腾讯云备案系统：https://console.cloud.tencent.com/beian
2. 填写主体信息（企业信息）
3. 填写网站信息
4. 上传材料：
   - 营业执照
   - 法人身份证
   - 网站负责人身份证
   - 核验照片（按指引拍摄）
5. 提交管局审核

备案时间：
- 腾讯云初审：1-2 天
- 管局审核：7-20 天（各省不同）
- 总计：约 10-25 天

### 3.4 域名解析

备案通过后，添加 DNS 解析：

| 主机记录 | 记录类型 | 记录值 |
|---------|---------|--------|
| @ | A | 你的服务器IP |
| www | A | 你的服务器IP |
| api | A | 你的服务器IP |

### ⚠️ 备案注意事项

- 备案期间网站不能访问
- 网站名称不能包含敏感词
- 个人备案限制较多，建议用企业备案
- 备案成功后，网站底部必须显示备案号

---

## 第四步：服务器部署

### 4.1 连接服务器

```bash
ssh root@你的服务器IP
```

### 4.2 安装 Docker

```bash
# 更新系统
apt update && apt upgrade -y

# 安装 Docker
curl -fsSL https://get.docker.com | sh

# 安装 Docker Compose
apt install docker-compose -y

# 验证安装
docker --version
docker-compose --version
```

### 4.3 上传项目代码

在本地执行：
```bash
# 打包项目
cd /Users/mzm/Desktop/News
tar -czvf news-backend.tar.gz backend/

# 上传到服务器
scp news-backend.tar.gz root@你的服务器IP:/root/
```

在服务器执行：
```bash
cd /root
tar -xzvf news-backend.tar.gz
cd backend
```

### 4.4 配置环境变量

创建 `.env` 文件：
```bash
cat > .env << 'EOF'
# 数据库
DATABASE_URL=mysql://ainews:你的数据库密码@mysql:3306/ainews

# 微信小程序
WECHAT_APP_ID=你的AppID
WECHAT_APP_SECRET=你的AppSecret

# GLM API
GLM_API_KEY=你的GLM密钥

# JWT
JWT_SECRET=随机生成一个长字符串

# 其他
CREATOR_CODE=你的管理员密码
EOF
```

### 4.5 创建 docker-compose.yml

```yaml
version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: ainews-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: 你的root密码
      MYSQL_DATABASE: ainews
      MYSQL_USER: ainews
      MYSQL_PASSWORD: 你的数据库密码
    volumes:
      - mysql_data:/var/lib/mysql
    ports:
      - "3306:3306"

  backend:
    build: .
    container_name: ainews-backend
    restart: always
    depends_on:
      - mysql
    env_file:
      - .env
    volumes:
      - ./storage:/app/storage
    ports:
      - "8000:8000"

  nginx:
    image: nginx:alpine
    container_name: ainews-nginx
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
      - ./storage/audio:/var/www/audio

volumes:
  mysql_data:
```

### 4.6 配置 Nginx

创建 `nginx.conf`：
```nginx
events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    server {
        listen 80;
        server_name 你的域名.com;

        # 强制跳转 HTTPS
        return 301 https://$server_name$request_uri;
    }

    server {
        listen 443 ssl;
        server_name 你的域名.com;

        ssl_certificate /etc/nginx/ssl/fullchain.pem;
        ssl_certificate_key /etc/nginx/ssl/privkey.pem;

        # API 代理
        location /api/ {
            proxy_pass http://backend:8000;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }

        # 音频文件
        location /audio/ {
            alias /var/www/audio/;
            add_header Access-Control-Allow-Origin *;
        }
    }
}
```

### 4.7 配置 SSL 证书

```bash
# 安装 certbot
apt install certbot -y

# 获取证书（先停止 nginx）
docker-compose stop nginx
certbot certonly --standalone -d 你的域名.com

# 复制证书
mkdir -p ssl
cp /etc/letsencrypt/live/你的域名.com/fullchain.pem ssl/
cp /etc/letsencrypt/live/你的域名.com/privkey.pem ssl/

# 设置自动续期
echo "0 0 1 * * certbot renew --quiet && docker-compose restart nginx" | crontab -
```

### 4.8 启动服务

```bash
# 构建并启动
docker-compose up -d --build

# 查看日志
docker-compose logs -f

# 检查状态
docker-compose ps
```

### 4.9 验证部署

```bash
# 测试 API
curl https://你的域名.com/api/v1/news

# 应该返回 JSON 数据
```

---

## 第五步：小程序前端开发

### 5.1 安装开发工具

1. 下载微信开发者工具：
   https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html

2. 安装并登录微信账号

### 5.2 创建项目

1. 打开开发者工具
2. 新建项目
3. 填写：
   - 项目名称：AI资讯
   - 目录：选择一个空文件夹
   - AppID：填写你的 AppID
   - 开发模式：小程序
   - 后端服务：不使用云服务

### 5.3 配置服务器域名

在小程序后台配置：
1. 开发管理 → 开发设置 → 服务器域名
2. 添加：
   - request 合法域名：`https://你的域名.com`
   - downloadFile 合法域名：`https://你的域名.com`

### 5.4 项目结构

我会帮你创建以下文件：

```
miniprogram/
├── app.js              # 入口文件
├── app.json            # 全局配置
├── app.wxss            # 全局样式
├── pages/
│   ├── index/          # 首页（新闻列表）
│   ├── detail/         # 新闻详情
│   ├── audio/          # 播客列表
│   └── settings/       # 设置页
├── components/
│   ├── news-card/      # 新闻卡片组件
│   └── player/         # 播放器组件
└── utils/
    ├── api.js          # API 封装
    └── util.js         # 工具函数
```

### 5.5 核心代码示例

**app.js**（入口）：
```javascript
App({
  globalData: {
    baseUrl: 'https://你的域名.com',
    token: null,
    userInfo: null
  },

  onLaunch() {
    // 自动登录
    this.login();
  },

  async login() {
    const { code } = await wx.login();
    const res = await wx.request({
      url: `${this.globalData.baseUrl}/api/v1/auth/login`,
      method: 'POST',
      data: { code }
    });
    this.globalData.token = res.data.access_token;
  }
});
```

**utils/api.js**（API 封装）：
```javascript
const app = getApp();

const request = (url, options = {}) => {
  return new Promise((resolve, reject) => {
    wx.request({
      url: `${app.globalData.baseUrl}${url}`,
      header: {
        'Authorization': `Bearer ${app.globalData.token}`,
        'Content-Type': 'application/json'
      },
      ...options,
      success: (res) => resolve(res.data),
      fail: reject
    });
  });
};

module.exports = {
  getNews: (params) => request('/api/v1/news', { data: params }),
  getNewsDetail: (id) => request(`/api/v1/news/${id}`),
  getAudioList: () => request('/api/v1/audio'),
  // ... 其他 API
};
```

---

## 第六步：测试与调试

### 6.1 本地调试

1. 开发者工具 → 详情 → 本地设置
2. 勾选「不校验合法域名」
3. 这样可以在备案完成前测试

### 6.2 真机调试

1. 开发者工具 → 预览
2. 手机微信扫码
3. 测试各项功能

### 6.3 测试清单

- [ ] 微信登录是否正常
- [ ] 新闻列表是否加载
- [ ] 新闻详情是否显示
- [ ] 分类筛选是否工作
- [ ] 音频播放是否正常
- [ ] 后台播放是否正常
- [ ] 设置保存是否生效
- [ ] 网络异常是否有提示

### 6.4 添加体验成员

1. 小程序后台 → 成员管理
2. 添加体验成员（最多 100 人）
3. 体验成员可以扫码体验未上线版本

---

## 第七步：提交审核

### 7.1 准备材料

1. **小程序名称**
   - 不能与已有小程序重名
   - 不能包含敏感词
   - 建议：AI资讯、智能资讯、科技快讯 等

2. **小程序简介**
   - 20-120 字
   - 描述主要功能

3. **小程序类目**
   - 推荐：工具 → 信息查询
   - 或：资讯 → 资讯阅读（可能需要资质）

4. **隐私协议**
   - 必须有隐私协议页面
   - 说明收集哪些用户信息

### 7.2 提交审核

1. 开发者工具 → 上传
2. 填写版本号和备注
3. 小程序后台 → 版本管理 → 提交审核
4. 填写审核信息

### 7.3 审核时间

- 普通审核：1-7 天
- 加急审核：需要额外申请

### 7.4 常见拒绝原因

1. **内容来源不明**
   - 解决：在「关于」页面说明内容来源

2. **诱导分享**
   - 解决：不要有「分享得积分」等功能

3. **类目不符**
   - 解决：选择正确的类目

4. **功能不完整**
   - 解决：确保所有页面都能正常使用

---

## 常见问题

### Q1: 小程序如何调用自己的服务器？

微信小程序通过 HTTPS 请求调用你的服务器 API：

```
小程序 → HTTPS请求 → 你的域名 → Nginx → 后端API
```

要求：
1. 必须是 HTTPS（443端口）
2. 域名必须已备案
3. 域名必须在小程序后台配置

### Q2: 不备案可以吗？

**不可以。** 微信小程序强制要求：
- 所有请求域名必须已备案
- 开发阶段可以勾选「不校验域名」临时测试
- 上线审核时必须使用已备案域名

### Q3: 备案需要多久？

- 腾讯云初审：1-2 天
- 管局审核：7-20 天
- 建议：先买服务器和域名，立即开始备案

### Q4: 音频功能会影响审核吗？

可能会。音频类小程序可能需要：
- 《信息网络传播视听节目许可证》
- 或《网络文化经营许可证》

建议：
1. 先上线纯文字版本
2. 审核通过后再添加音频功能
3. 或者将音频功能作为「播客收听」而非「内容发布」

### Q5: 服务器配置够用吗？

2核2G 对于你的项目完全够用：
- 预计日活：< 1000 人
- API 请求：< 10000/天
- 音频存储：< 10GB

如果后期用户增长，可以随时升级配置。

### Q6: 数据会丢失吗？

建议配置自动备份：
```bash
# 每天凌晨备份数据库
0 3 * * * docker exec ainews-mysql mysqldump -u root -p密码 ainews > /backup/ainews_$(date +%Y%m%d).sql
```

### Q7: 如何更新代码？

```bash
# 在服务器上
cd /root/backend
git pull  # 或重新上传代码
docker-compose up -d --build
```

---

## 时间规划

| 阶段 | 预计时间 | 说明 |
|------|---------|------|
| 注册小程序 | 1 天 | 包括认证 |
| 购买服务器 | 1 天 | |
| 域名备案 | 10-25 天 | **最耗时** |
| 服务器部署 | 1 天 | 我来帮你 |
| 小程序开发 | 3-5 天 | 我来帮你 |
| 测试调试 | 2-3 天 | |
| 提交审核 | 1-7 天 | |
| **总计** | **约 3-6 周** | 主要等备案 |

---

## 下一步行动

### 你需要做的：

1. **立即开始**
   - [ ] 注册微信小程序账号
   - [ ] 完成企业认证（300元）
   - [ ] 获取 AppID 和 AppSecret

2. **同时进行**
   - [ ] 购买腾讯云轻量服务器
   - [ ] 购买域名
   - [ ] 提交 ICP 备案

3. **备案期间**
   - 把 AppID、AppSecret、服务器IP 告诉我
   - 我来完成后端部署和小程序开发

### 我会帮你做的：

1. 修改后端代码适配自建部署
2. 编写 docker-compose 一键部署脚本
3. 开发微信小程序前端
4. 编写部署和运维脚本

---

## 联系方式

如有问题，随时问我。

文档版本：v1.0
更新日期：2024年
